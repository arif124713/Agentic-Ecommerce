# BlackCart backend — runtime dependencies only (what actually ships in the deployed Vercel
# function). Dev/test/tooling and ingestion-only packages live in requirements-dev.txt instead —
# Vercel's own Python runtime guidance is explicit about this ("only list packages necessary at
# runtime"), and a real deploy surfaced why: a bundle large enough to trigger Vercel's automatic
# size optimization silently dropped an unrelated package (app.api.v1.public failed to import) —
# trimming ~230MB of numpy/Pillow/pytest/mypy/etc. down to just what app/ actually imports avoids
# that optimization path entirely instead of hoping it never misfires again.
fastapi==0.140.0
starlette==1.3.1
sqlalchemy[asyncio]==2.0.36
pydantic==2.13.4  # bumped from 2.10.3 for the chat feature: mcp==1.29.0 requires pydantic>=2.11
email-validator==2.3.0
pydantic-settings==2.6.1
aiomysql==0.3.2
cryptography==50.0.0
python-dotenv==1.2.2
argon2-cffi==23.1.0
pyjwt==2.13.0
pyotp==2.10.0
qrcode==8.2
structlog==24.4.0
prometheus-fastapi-instrumentator==8.1.0
python-ulid==2.7.0
python-multipart==0.0.32
httpx==0.27.2
boto3==1.43.61
algoliasearch==4.44.4
# Chat feature (chat_spec.md): DeepSeek's OpenAI-compatible endpoint, and the MCP CLIENT side of
# the bridge (app/agents/) that talks to the 4 Railway-hosted MCP servers. The `mcp` SERVER
# authoring code lives in app/mcp/ but deploys separately (backend/requirements-mcp.txt) — the
# import is shared here only because MCP_Client_Pool needs the same package's client classes.
openai==1.59.9
mcp==1.29.0

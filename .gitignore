# Secrets
.env
.env.local
*.pem

# Python
myenv/
backend/**/__pycache__/
*.pyc
backend/.pytest_cache/
backend/.ruff_cache/
backend/.mypy_cache/
backend/.coverage
backend/htmlcov/

# Node
frontend/node_modules/
frontend/dist/

# Dataset (large, regeneratable via scripts/ingest_flipkart.py)
data/raw/
data/quarantine/

# Self-hosted product media (large, regeneratable via `python scripts/ingest.py --stage media`)
backend/media/

# Local DB backups (spec §26 — real dumps of local dev data, never a distributable artifact)
backend/backups/

# Resumability checkpoint for the one-time local-media -> remote storage migration
backend/scripts/.media_migration_checkpoint.txt

# Editor/OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Claude Code local session state (personal, machine-specific)
.claude/

.vercel
.env*
